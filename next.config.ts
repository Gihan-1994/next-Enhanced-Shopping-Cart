import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
    images: {
        domains: ['media.wired.com' , 'images.unsplash.com','media.istockphoto.com','plus.unsplash.com'],// Add the external domain here
    },
};

export default nextConfig;
